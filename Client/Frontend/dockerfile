# 1️⃣ Use a lightweight Python image
FROM python:3.11-slim

# 2️⃣ Set working directory
WORKDIR /app

# 3️⃣ Copy dependency file first (for Docker layer caching)
COPY requirements.txt .

# 4️⃣ Install dependencies
RUN pip install --no-cache-dir -r requirements.txt

# 5️⃣ Copy the rest of the application
COPY . .

# 6️⃣ Expose Streamlit’s default port
EXPOSE 8501

# 7️⃣ Set environment variable to allow external access
ENV STREAMLIT_SERVER_HEADLESS=true
ENV STREAMLIT_SERVER_ENABLECORS=false
ENV STREAMLIT_SERVER_ADDRESS=0.0.0.0
ENV STREAMLIT_SERVER_PORT=8501

# 8️⃣ Start Streamlit
CMD ["streamlit", "run", "frontend.py"]
